<plugin>
  <groupId>org.jacoco</groupId>
  <artifactId>jacoco-maven-plugin</artifactId>
  <version>0.8.10</version>

  <executions>

    <!-- 1. Attach JaCoCo agent before tests -->
    <execution>
      <id>prepare-agent</id>
      <goals>
        <goal>prepare-agent</goal>
      </goals>
    </execution>

    <!-- 2. Generate report AFTER all tests finish -->
    <execution>
      <id>report</id>
      <phase>verify</phase>   ✅ FIXED
      <goals>
        <goal>report</goal>
      </goals>
    </execution>

  </executions>
</plugin>
